auto-reload yes
color info cyan default
color listnormal white default
color listnormal_unread white default bold
color listfocus cyan default standout
color listfocus_unread cyan default bold standout
download-timeout 60
download-retries 10
bind-key ^O open-all-unread-in-browser-and-mark-read

# vi keybinds from Luke Smith's config
bind-key j down
bind-key k up
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key G end
bind-key g home
bind-key d pagedown
bind-key u pageup
bind-key l open
bind-key h quit
bind-key a toggle-article-read
bind-key n next-unread
bind-key N prev-unread
bind-key D pb-download
bind-key U show-urls
bind-key x pb-delete

bind-key | delete-article

browser "qutebrowser-open %u -l warning"
